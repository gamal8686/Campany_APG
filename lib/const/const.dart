import 'package:flutter/material.dart';

class AppColors {
  // درجات الرمادي
  static const Color grayLight = Color(0xFFF5F5F5); // رمادي فاتح جداً
  static const Color gray = Color(0xFFBDBDBD); // رمادي متوسط
  static const Color grayDark = Color(0xFF616161); // رمادي غامق

  // درجات البني الفاتح
  static const Color lightBrown = Color(0xFFD7CCC8); // بني فاتح جداً
  static const Color brown = Color(0xFFA1887F); // بني متوسط
}

const Color kbacegGrond = Color.fromARGB(77, 243, 239, 239);
const Color kText = Color.fromARGB(255, 12, 12, 12);
const Color darkBrown = Color(0xFF8B4513);

// الأخضر الزيتوني
const Color kmontg = Color.fromARGB(255, 3, 84, 23);
const Color klightBlue = Color(0xFF87CEFA);
const Color kbotton = AppColors.brown;

// الأزرق الفاتح

const Color darkBlue = Color(0xFF4682B4);
const Color kRed = Color.fromARGB(255, 197, 52, 52);
final kbacgrawenfdh = Colors.grey[800]; // أو أي لون بتستخدمه في الثيم
